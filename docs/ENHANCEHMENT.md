So for your human vs. fly reaction-time game, I would probably model the fly at around 20 to 50 ms for a simple escape reflex, rather than claiming one exact number. That comparison could actually be quite dramatic because a typical human visual reaction time in a simple button-press

task is around 200 to 250 ms. So you could reasonably visualize something like: Fruit fly: ~25 ms Human: ~200 to 250 ms Difference: roughly 8 to 10× faster for the fly's rapid escape pathway.
One important caveat for your demo: the ~22 ms isn't measured from the instant an object first appears. It's measured once the looming object reaches a visual threshold that triggers the escape circuitry. That distinction matters if we're designing the game scientifically.
